{
  "name": "teste-qa-phoebus",
  "version": "1.0.0",
  "scripts": {
    "test": "newman run postman/collections.json -e postman/environment.json -r html,json --reporter-html-export reports/report.html",
    "test:verbose": "newman run postman/collections.json -e postman/environment.json -r cli,html,json --reporter-html-export reports/report.html"
  },
  "devDependencies": {
    "newman": "^latest",
    "newman-reporter-html": "^latest"
  }
}